let weight: Double = 300
let height: Double = 5.9
var bmi: Double = weight / (height * height) // height squared? but the number isn't right
print(bmi)

// this isn't right, on the math end, not sure how to fix will come back later
